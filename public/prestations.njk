---
title: Tarifs et prestations
layout: layout.njk
prestations: active
---

<h1>{{title}}</h1>

<div class="cartes">

  {% for prestation in collections.prestations %}
  <div class="card">
    <div class="card-body">
      <h5 class="card-title">{{prestation.data.title}}</h5>
      <div class="card-desc">
        <p class="card-text">{{prestation.data.description}}</p>
        <a href="#" class="btn btn-secondary">{{prestation.data.price}}</a>
      </div>
    </div>
  </div>
  {% endfor %}

</div>